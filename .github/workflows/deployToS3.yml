name: Upload website to S3


on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_REGION: us-east-1   # optional: defaults to us-east-1
        SOURCE_DIR: .      # optional: defaults to entire repository

# GitHub Actions Workflow: Deploy Static Website to S3
# Purpose: Automatically syncs static website files to an AWS S3 bucket
# Trigger: Runs on specified events (push, pull_request, etc.)
# 
# Jobs:
#   - deploy: Executes on ubuntu-latest runner
#     - Checks out the repository code
#     - Configures AWS credentials using stored secrets
#     - Syncs local files to S3 bucket, excluding .github directory
#     - Deletes S3 objects that no longer exist locally
#
# Requirements:
#   - AWS_ACCESS_KEY_ID secret configured in repository settings
#   - AWS_SECRET_ACCESS_KEY secret configured in repository settings
#   - S3 bucket must already exist in AWS account
#   - IAM user must have s3:GetObject, s3:PutObject, s3:DeleteObject permissions
# jobs:  
#   deploy:
#     runs-on: ubuntu-latest 
# steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1

#       - name: Sync files to S3
#         run: |
#           aws s3 sync . s3://your-s3-bucket-name --exclude ".github/*" --delete      
    
